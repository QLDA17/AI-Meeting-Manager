# Worklog 2026-05-25

## Muc tieu
Tong hop nhung gi da duoc lam tu toi qua den hien tai, tap trung vao:
- admin workflow audit fix
- AI Notes / summary / action item flow
- meeting scheduling flow va meeting list
- danh sach test da them va da chay

## 1. Admin Workflow Audit Fix

### Da sua backend
- Chuyen admin runtime state tu RAM/file sang DB-backed:
  - `ADMIN_SYSTEM_SETTINGS`
  - `ADMIN_PROMPTS`
  - `ADMIN_BROADCAST_HISTORY`
- Them `maintenance middleware`:
  - bat `maintenance_mode` thi request thuong bi chan `503`
  - van cho `system-admin`, login va health check
- Chuan hoa admin user management:
  - chan tu ha quyen chinh minh
  - chan tu vo hieu hoa chinh minh
  - chan vo hieu hoa system admin cuoi cung
- Chuan hoa `DELETE /api/admin/users/{id}` theo nghia deactivate, khong phai hard delete
- Them audit log cho:
  - login success
  - login fail
  - password reset / password change
  - export transcript

### Da sua frontend
- Sua `RoleGuard` de `system-admin` khong auto-pass moi role frontend theo kieu hierarchy ngam
- Route `/groups/create` chi con cho role phu hop
- An link `/org/admin` voi user khong du quyen
- Bo route trung `/create`, chi giu `/meetings/create`
- Bo menu `Tu dien AI` khoi admin layout
- Don admin dashboard, bo metric/mock UI gay hieu nham
- Bo `require_2fa_admin` khoi settings UI/API payload
- Doi wording admin users:
  - `role` -> `Vai tro he thong`
  - `delete user` -> `Vo hieu hoa tai khoan`
- Them auto logout frontend khi API tra `403 Account is deactivated`

## 2. Org Audit Log

### Da them backend
- Them API `GET /api/organizations/{org_id}/audit-logs`
- Mo rong luong ghi audit de event org/group/meeting/action item ghi dung `org`
- Scope org-admin xem duoc log dung theo organization

### Da them frontend
- Them tab `Nhat ky` trong `OrgAdminConsole`
- Them UI `OrgAuditLogsTab` de hien thi org-scoped audit logs

## 3. AI Notes / Summary / User Language Preference

### Backend da sua
- `meeting detail` tra them:
  - `preferred_summary_language`
  - `meeting_default_summary_language`
  - `available_summary_languages`
  - `summary_generation_state`
- Uu tien `current_user.language` khi chon AI Notes language
- Khong hardcode `vi` cho luong finalize / upload
- `generate_action_items` mac dinh la `false` cho:
  - live finalize
  - upload audio
  - regenerate AI Notes
- Regenerate voi `generate_action_items=true` se:
  - thay AI-generated tasks cu cua generation do
  - giu nguyen manual tasks
- Ho tro summary da ngon ngu theo pack:
  - `vi`
  - `en`
  - `zh`
  - `ja`
  - `ko`

### Frontend da sua
- `MeetingDetail` default theo `preferredSummaryLanguage`
- Hien thi ro trang thai `PROCESSING` / `MISSING` / fallback thay vi nga ngam ve `vi`
- Nut duoc doi semantic thanh `Gen lai AI Notes full`
- Them toggle `Sinh AI tasks`, mac dinh tat
- Manual tasks hien on dinh, AI tasks moi filter theo summary/language
- `MeetingRoom` va `useAudioRecorder` bo hardcode language `vi`

## 4. Meeting Scheduling Flow va Meeting List

### Backend da sua
- `POST /api/meetings`
  - meeting non-live bat buoc co `scheduled_start`
  - giu rule chan meeting qua khu, end <= start, overlap
- `PUT /api/meetings/{id}`
  - chan sua meeting `completed/canceled/failed`
  - sua compare datetime naive/aware
- `GET /api/meetings`
  - sort mac dinh theo:
    1. `scheduled_start`
    2. `actual_start`
    3. `created_at`
- `scheduler`
  - khong set `meeting.status` truc tiep nua
  - dung `update_meeting()` cho:
    - `upcoming -> live`
    - `live -> completed`
  - invalid transition thi log warning va skip

### Frontend da sua
- `EditMeetingModal`
  - validate client-side khi doi meeting ve thoi gian qua khu
- `MeetingCard`
  - sua logic `isStartingSoon`
  - meeting upcoming da qua gio khong con bi coi la joinable
- `MeetingList`
  - them CTA `Lich hop`
  - CTA mo flow `ScheduleMeetingModal` qua `useCalendarStore`
  - empty state cung co loi vao schedule

## 5. Test Da Them

### Backend
- `backend/tests/api/routes/test_admin_api.py`
- `backend/tests/api/routes/test_auth_register.py`
- `backend/tests/api/routes/test_organizations_api.py`
- `backend/tests/api/routes/test_meeting_api.py`
  - them cac test moi cho scheduling va AI Notes:
    - `test_create_upcoming_meeting_requires_scheduled_start`
    - `test_update_meeting_rejects_past_scheduled_start`
    - `test_update_meeting_rejects_completed_meeting`
    - `test_get_meetings_sorted_by_meeting_time_desc`
    - `test_meeting_detail_prefers_current_user_language`
    - cac test finalize / export / upload lien quan
- `backend/tests/api/test_scheduler.py`
  - `test_status_transition_loop_uses_valid_update_path`

### Frontend
- `frontend/tests/App.admin-routing.test.tsx`
- `frontend/tests/components/layout/Sidebar.test.tsx`
- `frontend/tests/pages/org/OrgAdminConsole.test.tsx`
- `frontend/tests/pages/MeetingDetail.ai-notes.test.tsx`
- `frontend/tests/services/mappers.test.ts`
- `frontend/tests/components/meeting/MeetingCard.test.tsx`
- `frontend/tests/components/meeting/EditMeetingModal.test.tsx`
- `frontend/tests/pages/MeetingList.test.tsx`

## 6. Test Da Chay va Ket Qua

### Backend da chay pass
```bash
PYTHONPATH=backend ./backend/.venv/bin/python -m pytest backend/tests/api/routes/test_admin_api.py backend/tests/api/routes/test_auth_register.py -q
```
- Ket qua: `14 passed`

```bash
PYTHONPATH=backend ./backend/.venv/bin/python -m pytest backend/tests/api/routes/test_admin_api.py backend/tests/api/routes/test_auth_register.py backend/tests/api/routes/test_organizations_api.py -q
```
- Ket qua: `20 passed`

```bash
PYTHONPATH=backend ./backend/.venv/bin/python -m pytest backend/tests/api/routes/test_meeting_api.py -q
```
- Ket qua: `19 passed`

```bash
PYTHONPATH=backend ./backend/.venv/bin/python -m pytest backend/tests/api/routes/test_meeting_api.py backend/tests/api/test_scheduler.py -q
```
- Ket qua: `25 passed`

### Frontend da chay pass
```bash
cd frontend && npm run test -- App.admin-routing.test.tsx Sidebar.test.tsx
```
- Ket qua: `4 passed`

```bash
cd frontend && npm run test -- MeetingDetail.ai-notes.test.tsx mappers.test.ts App.admin-routing.test.tsx Sidebar.test.tsx OrgAdminConsole.test.tsx
```
- Ket qua: `13 passed`

```bash
cd frontend && npm run test -- MeetingList.test.tsx MeetingCard.test.tsx EditMeetingModal.test.tsx
```
- Ket qua: `3 passed`

```bash
cd frontend && npm run test
```
- Ket qua da ghi nhan truoc do: `11 passed, 41-42 tests passed` tuy theo thoi diem suite

### Build da chay pass
```bash
cd frontend && npm run build
```
- Ket qua: pass

## 7. Trang Thai Server Hien Tai

### Dang chay
- Backend: `http://127.0.0.1:8000`
- Frontend: `http://127.0.0.1:5173`

## 8. Residual Notes
- Backend test van con mot so warning cu:
  - startup test env co log MySQL khong anh huong ket qua route tests
  - `SAWarning` lien quan `IN (subquery)`
  - mot so deprecation warning ngoai scope fix hien tai
- Backlog chua lam trong dot nay:
  - `storage_limit_gb_per_org`
  - `transcript_retention_policy`
  - `Glossary`
  - `view as org/user`
  - nhung mo rong sau cua org audit log
