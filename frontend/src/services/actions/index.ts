/**
 * Barrel export for the FE Action Layer. UI code (components, pages, hooks)
 * MUST import from here, not from `data/*` or `lib/mockStore/*` directly.
 *
 * Phase 0 surface intentionally small — extend as each entity is migrated in
 * later phases (see `PLANS/PLAN_FE_SCHEMA_ALIGNMENT.md`).
 */

export * from './userActions';
